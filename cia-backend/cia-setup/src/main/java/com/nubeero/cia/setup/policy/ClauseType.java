package com.nubeero.cia.setup.policy;

/** Classification of a policy clause. Matches the frontend clause-types.ts union. */
public enum ClauseType {
    STANDARD, EXCLUSION, SPECIAL_CONDITION, WARRANTY
}
