apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cia-backend.fullname" . }}-env
  labels:
    {{- include "cia-backend.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
